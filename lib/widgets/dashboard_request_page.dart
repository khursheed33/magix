import 'package:flutter/cupertino.dart';

class DashboardRequestPage extends StatelessWidget {
  const DashboardRequestPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("REQUESTS"),
    );
  }
}
