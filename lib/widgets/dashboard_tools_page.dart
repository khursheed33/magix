import 'package:flutter/cupertino.dart';

class DashboardToolsPage extends StatelessWidget {
  const DashboardToolsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("TOOLS"),
    );
  }
}
